tailscale: true

enable: true

# valkey/valkey:9-alpine runs as a fixed non-root user (uid 999, gid 1000).
# The bind-mounted host directory must be owned to match, or valkey can't
# write its RDB snapshot.
runtime_directories:
  - path: valkey
    owner: 999
    group: 1000
