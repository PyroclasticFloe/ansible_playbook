# ---> Ansible
*.retry
.ansible/
services/*/var/**
!services/*/var/.keep
# Jellyfin ships seed data in var (cache + config) that must be installed
# with the service, so track it explicitly.
!services/jellyfin/var/**

ansible/host_vars/*.retry
*.retry

*.swp
*~

# Tailscale auth key (plain secret, read directly by Ansible)
/tailscale.env

# for files I don't want in git but need termporarily
temp_files/
