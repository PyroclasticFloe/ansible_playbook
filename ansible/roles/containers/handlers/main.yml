---
- name: Restart service
  ansible.builtin.set_fact:
    changed_units: "{{ (changed_units | default([])) + (systemd_units if start_service else []) }}"

- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
